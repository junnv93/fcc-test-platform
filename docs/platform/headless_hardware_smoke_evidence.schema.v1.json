{
  "schema_version": 1,
  "status": "manual_evidence_contract",
  "required": [
    "schema_version",
    "db_path",
    "job",
    "events",
    "artifacts",
    "report_requests"
  ],
  "job_required": [
    "id",
    "status",
    "excel_path",
    "session_id"
  ],
  "required_job_status": "completed",
  "required_event_types": [
    "job_execution_started",
    "provider_session_starting",
    "provider_session_completed",
    "job_execution_completed"
  ],
  "artifact_required_any_path": [
    "relative_path",
    "path_or_uri"
  ],
  "required_report_status": "completed",
  "required_report_fields": [
    "generated_paths_json"
  ],
  "path_rules": [
    "artifact relative_path/path_or_uri and generated report paths must be safe relative paths",
    "absolute paths, drive-qualified paths, and parent traversal are invalid"
  ],
  "notes": "This schema describes the evidence bundle collected after a real Unlicensed hardware smoke run. Automated tests validate the evidence format but do not replace the lab run."
}
