{
  "$id": "performance_smoke_evidence.schema.v1.json",
  "schema_version": 1,
  "status": "planning_contract",
  "owner_repository": "fcc-test-platform",
  "description": "Evidence manifest for safe read-route performance smoke results before provider cutover.",
  "required_top_level": [
    "schema_version",
    "provider_id",
    "benchmark_id",
    "collected_at",
    "target_base_url",
    "iterations",
    "workers",
    "max_p95_ms",
    "compatible",
    "within_budget",
    "total_failures",
    "routes"
  ],
  "required_routes": [
    "/health",
    "/headless/api-contract",
    "/headless/capabilities"
  ],
  "route_required_fields": [
    "route",
    "requests",
    "successes",
    "failures",
    "min_ms",
    "mean_ms",
    "p95_ms",
    "max_ms"
  ],
  "rules": [
    "compatible must be true",
    "within_budget must be true",
    "total_failures and each route failures value must be 0",
    "each required safe read route must be present",
    "each route p95_ms must be <= max_p95_ms"
  ],
  "notes": "This validator checks captured benchmark evidence only. It does not start services, call remote networks, run hardware, or generate load by itself."
}
