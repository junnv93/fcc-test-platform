/**
 * id_token 검증에 쓰는 `jose` 표면 — **오직 이 둘뿐**.
 *
 * ⚠️ 이 모듈이 존재하는 이유는 재수출이 아니라 **트리셰이킹 경계**다.
 *
 * `oidc-pkce.ts` 는 이 모듈을 동적으로(`import('./jose-verify')`) 부른다. 만약
 * 거기서 `import('jose')` 를 직접 불렀다면 동적 import 는 **네임스페이스 전체**를
 * 요구하므로 Rollup 이 무엇을 버려도 되는지 알 수 없고, jose 패키지가 통째로
 * 딸려 온다 — 실측 2026-09-05: 그 형태에서 비동기 청크가 gzip **16,960 B**
 * (모듈 78개)였고 상자 총계가 **+10,060 B** 늘었다.
 *
 * 정적 import 를 이 안에 가두면 경계 안쪽은 평범한 정적 그래프라 트리셰이킹이
 * 그대로 돈다. 바깥에서 지연되는 것은 여전히 같고, 안에서 남는 것은 실제로 쓰는
 * 두 함수의 그래프뿐이다.
 *
 * 그러므로 여기에 **쓰지 않는 것을 재수출하지 마라.** 한 줄 더하는 순간 그 줄이
 * 끌고 오는 그래프가 이 청크에 들어온다.
 */
export { createRemoteJWKSet, jwtVerify } from 'jose';
