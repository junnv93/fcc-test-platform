import { ConsoleHome } from './console-home';

/**
 * `/combined` — 전체 총괄.
 *
 * ┌─ 목적 ────────────────────────────────────────────────────────────────┐
 * │ 누가   엔지니어 관리자 (현장과 일정을 함께 책임지는 사람)              │
 * │ 언제   둘 사이의 «어긋남»을 볼 때                                      │
 * │ 무엇에 답하나                                                          │
 * │        · 진행이 늦은 밴드가 «지금 측정 중인» 것과 같은가               │
 * │        · 구성이 놀고 있는데 남은 case 가 쌓여 있지 않은가              │
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * ⚠️ 이 화면은 «둘의 합»이지 «셋째 화면»이 아니다. 여기에만 있는 위젯을 만들면
 * 세 홈이 서로 다른 말을 하기 시작하고, 그때부터 어느 화면이 맞는지 아무도
 * 모른다. 새 위젯은 현장이나 진행 중 «어느 쪽의» 것인지 먼저 정한다.
 */
export function CombinedOverviewRoute(): JSX.Element {
  return <ConsoleHome scope="manager" />;
}

export default CombinedOverviewRoute;
