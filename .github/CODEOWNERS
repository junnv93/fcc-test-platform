# fcc-test-platform — CODEOWNERS
#
# ⚠️ 2026-08-31: 이 레포가 **정본**이 됐습니다. 이전 판본은 "모노레포에서 리뷰되고
#    납품 상자로 배송되므로 여기서 고치면 덮어써진다"고 적고 있었는데, 추출·배송
#    기계가 퇴역하면서 **그 문장은 거짓이 됐습니다.** 유일한 사본이 여기 있으므로
#    변경은 여기서 합니다.
#
# ⚠️ **핸들은 실재하는 계정 하나뿐입니다.** 모노레포 판본은
#    `@fcc-frontend-auth-reviewer` 같은 팀 핸들을 적고 있었으나 그 팀은
#    **GitHub 에 존재하지 않습니다** — 존재하지 않는 팀을 가리키는 CODEOWNERS 는
#    리뷰어를 배정하지 않으면서 배정된 것처럼 읽힙니다. 팀이 만들어지는 날
#    아래 핸들만 바꾸면 경로 구조는 그대로 씁니다.
#
# ⚠️ **이 파일은 아직 강제하지 않습니다.** 실제로 리뷰어를 요구하려면 브랜치 보호의
#    "Require review from Code Owners"가 켜져야 합니다. 오늘의 역할은 강제가 아니라
#    **어느 경로가 누구의 검토를 받아야 하는가의 선언**입니다.

# 인증 — 프론트엔드
/apps/web/src/auth/                   @junnv93
/apps/web/src/observability/sentry.ts @junnv93
/apps/web/tests/auth/                 @junnv93

# 인증 — 백엔드
# ⚠️ `oidc_principal_resolver` 는 이 레포에 없습니다 — `fcc-test-contracts` 로
#    갔습니다. 여기 없는 경로를 적으면 아무것도 지키지 않는 줄이 됩니다.
/application/central_contract/        @junnv93

# 배포·문서
/infra/                               @junnv93
/docs/architecture/frontend/          @junnv93

# 그 외 전부
*                                     @junnv93
