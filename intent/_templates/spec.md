<!--
  서식: spec.md — 「무엇을」
  쓰는 쪽: 에이전트 (승인된 intent.md 를 읽고)
  이 파일 하나만 담은 PR 을 올리세요.

  승인: **동료 1인**(작성자 제외). 리드는 상시 승인자가 아닙니다.
        아래 §4 `Debt-Accepted-By` 와 §7 `→ 답(이름):` 이 비어 있으면
        방아쇠 T2·T3 가 발화해 검사가 초록이 되지 않습니다.
        규칙 본문 → intent/README.md §방아쇠

  ⚠️ 이 파일은 요구사항만 적는 파일이 아닙니다. **회사 정책이 설계에 적용되는
     자리**입니다.
-->

# Spec: <intent 와 같은 제목>

Intent: ./intent.md
Author: Claude (draft) / <검토자>
Date: <YYYY-MM-DD>
Status: draft
Slug: <폴더 이름>

## 1. 요구사항

<intent 의 Proposed outcome 을 검증 가능한 문장들로 분해합니다.
 각 항목은 번호를 답니다 — plan.md 와 테스트가 이 번호를 인용합니다.>

- R1. …
- R2. …

## 2. 범위 밖 (Non-goals)

<하지 않을 것을 **명시적으로** 적습니다. 이 절이 없으면 구현 단계에서
 범위가 조용히 넓어집니다.>

## 3. 설계 결정과 근거

<선택지가 둘 이상이었던 지점마다: 무엇을 골랐고, 무엇을 버렸고, **왜**인지.
 버린 쪽을 적지 않으면 6개월 뒤 누가 그것을 「개선」이라며 되돌립니다.>

| # | 결정 | 버린 대안 | 근거 |
|---|---|---|---|
| D1 | | | |

## 4. 영향받는 경계

<이 레포의 구조상 반드시 답해야 하는 것들입니다.>

- **레인**: platform 만 / contracts 도 (그렇다면 커널 태그가 **먼저** 나가야 합니다)
- **DB**: 마이그레이션 필요 여부. 필요하면 중앙 DB 사전점검 대상입니다.
- **API 계약**: OpenAPI 표면이 바뀌는가. 바뀌면 아티팩트 재생성이 계획에 들어갑니다.
- **프론트엔드**: 시각 골든이 바뀌는가.
- **provider 비공개 레포**: 닿는가. 닿으면 이 레포에서 끝낼 수 없습니다.

**Debt-Accepted-By**: (없음)

<!-- delivered_test_run_baseline.json 에 실패 이름을 «추가»하는 계획이라면
     여기에 `Debt-Accepted-By: <이름> — <사유>` 를 적어야 합니다.
     비어 있으면 방아쇠 T2 가 발화합니다. 부채 등재는 사람의 결정입니다. -->

## 5. 정책 확인

<Skills 로 인코딩된 정책 중 이 스펙이 걸리는 것과, 그 판정.
 걸리는 것이 없으면 "해당 없음" 이 아니라 **무엇을 확인했는지**를 적으세요.>

- 인증/RBAC:
- 개인정보:
- 오프라인 동작:

## 6. 성공 판정

<intent 의 Success criteria 를 **어떻게 관측할 것인지**로 옮깁니다.
 어떤 테스트·스크립트·화면이 그 값을 내는지 이름으로 적으세요.>

## 7. 열린 질문

<답이 필요한 것을 적고, 답이 나오면 그 아래에 `→ 답(<이름>): <내용>` 을 답니다.

 ⚠️ `→ 답( ):` 이 **비어 있는 항목이 하나라도 남으면 방아쇠 T3 가 발화**해
    검사가 초록이 되지 않습니다. 답 없이 설계가 확정되는 것을 막는 축입니다.
    「없음」이 답이면 그것도 이름을 붙여 적으세요 — `→ 답(홍길동): 해당 없음`.>

1. 
   → 답( ): 
